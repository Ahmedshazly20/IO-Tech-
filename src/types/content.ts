export type Service = { id: number; title: string; description?: string };
export type TeamMember = { id: number; name: string; role?: string; avatar?: string };
export type BlogPost = { id: number; title: string; slug: string; content?: string };
export type Subscriber = { id: number; email: string };
